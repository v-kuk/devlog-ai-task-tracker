// TODO: Implement new task page
// - Render TaskForm in "create" mode
// - No <form> tags — use controlled inputs + onClick handlers
// - On success, redirect to /

export default function NewTaskPage() {
  return (
    <main>
      <p>TODO: New task page</p>
    </main>
  );
}
