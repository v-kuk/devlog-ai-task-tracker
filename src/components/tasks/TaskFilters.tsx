// TODO: Implement TaskFilters component
// - Reads current filter state from URL search params (useSearchParams)
// - Updates filters by pushing new URL params via useRouter (NO <form> tags)
// - Filter controls: status (Select), priority (Select), search (Input)
// - "Clear filters" button resets all params
// - Filters are the source of truth — never local state

"use client";

export function TaskFilters() {
  // TODO
  return <div>TODO: TaskFilters</div>;
}
